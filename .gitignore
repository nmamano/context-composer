node_modules/
*.log
.DS_Store

# Phase 2 durable store (daemon default + per-scenario stores) and atomic-write temps
.ctx-store*.json
.*.tmp-*

# §11 Phase 2.6 wiretap (raw wire evidence — contains conversation content)
.ctx-wiretap*.jsonl
